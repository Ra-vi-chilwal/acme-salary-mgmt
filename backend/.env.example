# Backend environment variables

# Node environment (development | production)
NODE_ENV=development

# API server port
PORT=4000

# Database file path (optional, defaults to ./salary.sqlite)
# DB_PATH=/data/salary.sqlite

# Seed count for development (defaults to 10000)
# SEED_COUNT=1000
